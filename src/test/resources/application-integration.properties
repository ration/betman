# PropertySource doesn't support YAML, so hello properties file, nice to see you again.
spring.jackson.serialization.write-dates-as-timestamps=false
db.url=jdbc:h2:mem:test
security.jwt.token.secret-key=integration-key
security.jwt.token.expire-length=integration-key

